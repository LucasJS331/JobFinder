<div id="job-view-container" class="container">
    <div class="row tex-center">
        <a class="back-link main-color" href="/">Voltar</a>
    </div>
    <div id="container-view">
        <div id="img-container" class="col-md-12">
            <img src="/img/company.svg" alt="company">
        </div>

        <div class="col-md-12">
            <h1 class="main-color">{{job.dataValues.titulo}}</h1>
            <p class="bold">Descrição da Vaga: </p>
            <p class="job-desc">{{job.dataValues.description}}</p>
            <p><span class="bold">O salario informado é de: </span> R$ {{job.dataValues.salary}}</p>

            <p>
            para trabalhar  na <span class="bold main-color"> {{job.dataValues.company}}</span> envie um email para <a  id="link-view" href="mail:to{{job.dataValues.email}}" target="blank" class="main-color"> {{job.dataValues.email}} </a>
            </p>

    </div>
</div>

</div>